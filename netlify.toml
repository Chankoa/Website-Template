[build]
  base = "www"
  command = "npm ci --include=optional || npm install --include=optional && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--include=optional"
